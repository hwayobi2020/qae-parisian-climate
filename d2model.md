# D−2 대기강 지속 예측 모델 — 설계 기록

> 마지막 갱신: 2026-07-04. 이 문서는 "D−2(2일 전) 예보로 대기강 24시간 지속을 예측하는" 모델의 전체 설계를 기록한다.

## 1. 목적 / 핵심 질문
- 대기강(AR, Atmospheric River = 대기 중 수증기가 강처럼 좁고 길게 수송되는 현상)이 **발생 2일 전(D−2) 시점**에서, D일에 **24시간 이상 지속**될지 예측한다.
- 핵심 질문: **우리 모델이 기상청 원예보(raw forecast)보다 이 지속 예측을 더 잘하는가?**
- 비교 기준(baseline)은 언제나 **원예보 그 자체**이지, 다른 모델이 아니다.

## 2. 데이터
| 요소 | 출처 | 기간 | 비고 |
|---|---|---|---|
| 예보 | GEFS v12 재예측(reforecast) | 2000~2019 (20년) | **D−2 00Z 발표, c00 컨트롤 1멤버**, 리드 48~90h |
| env(환경, 관측/재분석) | IVT·순환지수·기압 등 | 1980~2023 (44년) | 인코더 학습용 |
| 지역 | 3곳 | — | CA(캘리포니아), UK(영국), Chile(칠레) |

- **왜 D-2인가**: 근접예보(0~18h)는 예보가 관측과 상관 0.9로 거의 정답을 측정 → 천장(예보만으로 이미 강함)이라 모델이 얹힐 여지가 작다. D−2는 예보가 약해(관측상관 0.55~0.66) 모델이 기여할 여지가 크다.
- 온셋까지 리드 = 48 + 온셋시각(OH) 시간 = **약 2~2.75일 전 예보**.

## 3. 타깃(정답 y)
- AR 판정: 6시간 IVT > 250
- 온셋 s(대기강 시작 시점), 온셋시각 OH ∈ {0,6,12,18}시.
- **지속** = 온셋+6h·+12h·+18h의 관측 IVT가 모두 AR 수준(>250) 유지.
- **y = (지속시간 ≥ 24h) 이진 레이블.**

## 4. 기준선 (raw 예보)
- fcv = D−2 예보 IVT의 **온셋+6/+12/+18h 중 최소값**(min).
  - 왜 min: 24h 지속하려면 세 시점 IVT가 다 버텨야 하므로, 가장 낮은 지점(병목)이 "지속 여부"의 핵심 신호.
- **예보의 지속 예측 = `fcv ≥ 250`** (파라미터 없는 원예보 그대로, 고정 임계).

## 5. 우리 모델
- 입력: **D−2 예보피처 9개** (전부 c00 1멤버 궤적)
  1~5. IVT @ 온셋·+6·+12·+18·+24h
  6~8. 연속 3점(+6/+12/+18)의 min / mean / std(시간축)
  9. 기울기 = (온셋+24) − (온셋)
- 모델: **TabPFN 회귀(Regressor)** (사전학습된 트랜스포머, in-context fit — 그래디언트 학습 없음).
- 출력: **연속구간 관측 min IVT를 수치로 예측** (raw fcv와 같은 IVT 단위·의미). 회귀 타깃 = 관측 min IVT(온셋+6/12/18h).
- 판정: **예측 min IVT ≥ 250(THR) → 지속(발생), < 250 → 미지속.** raw와 완전히 같은 임계·규칙(확률 0.5 없음).
- 역할: TabPFN은 날씨를 예보하지 않는다. GEFS가 만든 예보값을 **어떻게 지속 판정으로 번역하느냐**만 학습한다(원예보 위의 후처리기/해석기). GEFS는 기준·모델 양쪽 밑에 공통으로 깔린다.

## 6. env 인코더 (최종 폐기)
- 동기: 예보 없는 pre-2000(1980~1999)의 관측 env→지속 지식을, 인코더로 압축해 2000~2019 예보모델에 얹으려 함(전이의 유일한 통로).
- 설계: env 44피처를 **D−2 컷**(모든 관측을 온셋 2일 전 s0−8에서 끝냄, 예보 시점과 정합)으로 만들어, pre-2000에서 MLP(hid=8, leak-free)로 학습 → 8차원 표현을 2000~2019에 피처로 투입.
- **결과: 안 더함. 세 설계 전부 부정** — LR / 분류-TabPFN / 회귀-TabPFN. 회귀에서 예보9+인코더8: CA F1 0.687→0.673(악화), UK·Chile 변화 없음(예측 그대로). → 최종 헤드라인 모델은 **예보피처만(env 없음)**.

## 7. 평가 방법
- **지표: F1** (양성=지속 클래스의 precision·recall 조화평균, TN 다수 제외). 
- **단일 임계 250(THR), 대칭**: raw `fcv ≥ 250` / 우리모델 `예측 min IVT ≥ 250`. 두 모델 다 IVT 수치를 내고 같은 250으로 자름 → 임의 임계(0.5) 없음.
- 교차검증: 워크포워드 폴드(테스트 5블록, 64일 임베고). 유의성: 지역별 부트스트랩 → 평균 Δ의 CI/P, 3지역 통합검정.

## 8. 결과 현황 (F1, 회귀 설계 · 단일 THR) — 2026-07-04
| 지역 | n | 양성률 | fcv-y 상관 | 기준 raw F1 | 우리모델 F1 | ΔF1 | 95%CI | P(Δ>0) |
|---|---|---|---|---|---|---|---|---|
| CA | 801 | 0.25 | +0.62 | 0.550 | 0.687 | **+0.138** | [+0.081, +0.196] | 1.000 |
| UK | 863 | 0.15 | +0.55 | 0.506 | 0.503 | −0.003 | [−0.083, +0.078] | 0.466 |
| Chile | 653 | 0.36 | +0.66 | 0.433 | 0.773 | **+0.340** | [+0.267, +0.419] | 1.000 |
| **통합** | | | | | | **+0.158** | [+0.115, +0.202] | 1.000 |
- CA·Chile 크게 유의(P 1.000), UK flat(−0.003). 통합 +0.158, P 1.000.

## 9. 이득의 정체 — 랭킹(AUC/AUPRC) 진단 결과
| 지역 | ΔF1 | raw AUC | 모델 AUC | ΔAUC | raw AUPRC | 모델 AUPRC | ΔAUPRC |
|---|---|---|---|---|---|---|---|
| CA | +0.138 | 0.889 | 0.900 | +0.012 | 0.734 | 0.779 | +0.045 |
| UK | −0.003 | 0.901 | 0.907 | +0.006 | 0.671 | 0.672 | +0.001 |
| Chile | +0.340 | 0.906 | 0.928 | +0.022 | 0.842 | 0.887 | +0.045 |

- **raw 랭킹이 이미 천장 근처(AUC 0.89~0.91)** → 모델이 랭킹으로 더 올릴 여지 작음(ΔAUC +0.01~0.02).
- **큰 F1 이득의 대부분 = 보정(calibration)이고, 보정은 유효한 기여다**: D−2 예보는 진폭이 눌려(damping) AR 정의 250에 그대로 대면 실제 지속을 "250 미만이라 아니다"로 놓친다. 모델이 관측 스케일로 되살려 성공률을 올림. 250은 **물리적 절대 기준이라 낮추지 않는다**(raw 임계 튜닝은 headline 아닌 부차 ablation).
- **단순 상수 이동이 아님(증거)**: 상수/임계 이동은 랭킹 불변 → AUPRC 불변인데, **ΔAUPRC=+0.045(CA·Chile)** → 궤적 의존 보정으로 순서도 약간 개선. 얇지만 실재(CI 미부여).
- **결론**: raw@250(AR 정의) 상대로 **크고 유의한 승리(통합 ΔF1 +0.158, P 1.000) — 실재**. 정체 = "damped D−2 예보를 학습으로 재보정" + 얇은 랭킹 개선. UK는 raw가 이미 맞아 flat.

## 파일 맵
- `export_transfer_feats.py` → transfer_{region}.npz (env 44, y, oday, s 온셋인덱스)
- `export_d2feat.py` → d2feat_{region}.npz (D−2 예보피처 9 + D2min)
- `export_d2env.py` → d2env_{region}.npz (D−2 컷 env 44, THR)
- `colab_d2_tabpfn.py` → Colab 실행 셀 (기준 raw fcv≥THR / 우리모델 TabPFN회귀 예측 min IVT≥THR, F1, 통합검정) — 회귀 설계 완료·실행됨
